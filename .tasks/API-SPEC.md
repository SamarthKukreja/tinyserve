# Working API Spec

Canonical API file:

- `docs/api-spec.md`

Implementation rule:

- When Wave 7 touches server behavior, implement to `docs/api-spec.md`.
- If this working copy drifts from `docs/api-spec.md`, the published `docs/api-spec.md` wins.

Current API scope:

- `GET /health`
- `POST /generate`
- Optional `GET /metrics`

